{
    "name": "notes-wind-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "build": "cd client && npm install && npm run build && cd .. && node scripts/finalize_build.js",
        "start": "cd server && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "compression": "^1.7.4",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "helmet": "^7.1.0",
        "morgan": "^1.10.0",
        "serverless-http": "^3.2.0"
    }
}